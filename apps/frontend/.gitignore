# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

tls/

nginx/
# wrangler dev local state
.wrangler/

# `wrangler types` output: 15 000 lines of runtime types that
# @cloudflare/workers-types already provides. Only the Env interface it
# generates is worth keeping, and that lives in worker/env.d.ts by hand.
worker-configuration.d.ts
