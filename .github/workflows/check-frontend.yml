name: check frontend

on:
  push:
    branches: [ main ]
    paths:
      - 'apps/frontend/**'
      - 'proto/**'
      - '.github/workflows/check-frontend.yml'
  pull_request:
    paths:
      - 'apps/frontend/**'
      - 'proto/**'
      - '.github/workflows/check-frontend.yml'
  workflow_dispatch:

defaults:
  run:
    working-directory: apps/frontend

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: apps/frontend/package-lock.json

      # `npm ci` rather than `npm install`, so a package.json edited without
      # regenerating the lockfile fails here instead of drifting silently.
      - run: npm ci

      - run: npm run lint

      # The build runs `tsc -b` first, so this covers typechecking too.
      - run: npm run build

      - run: npm test
